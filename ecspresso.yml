region: '{{ must_env `AWS_REGION` }}'
cluster: '{{ must_env `ECS_CLUSTER` }}'
service: '{{ must_env `BOT_NAME` }}'
service_definition: ecs-service-def.json
task_definition: ecs-task-def.json
timeout: "10m0s"
